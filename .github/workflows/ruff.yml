name: Ruff
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - dev
jobs:
  ruff:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        with:
          args: "format --check --diff"

